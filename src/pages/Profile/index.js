import React from "react"

export default class Profile extends React.Component{
    render() {
        return (
            <div>
                这是Profile组件的内容,属于Home的子路由
            </div>
        )
    }
}